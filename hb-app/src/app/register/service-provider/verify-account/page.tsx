//import { verifyToken } from "@/lib/utils";

export default async function verifyServiceProviderAccount(token: string) {
  return (
    <div>
      <h1> Enter your 6 digit token</h1>

      {/* /** Verify token (this may require extra properties for the Account model)
      Redirect user to their profile */}
    </div>
  );
}
