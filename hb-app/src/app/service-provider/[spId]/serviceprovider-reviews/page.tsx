import React from "react";
import { Params } from "../../../../../types";

const ServiceProviderReviews = ({ params }: { params: Params }) => {
  return <div>Service Provider Reviews ${params.spId}</div>;
};

export default ServiceProviderReviews;
