export default function MaintenanceCalendar() {
  /**
   * Users can set preferences for when they have services completed
   * Can set reminders for users from their last appointment data for when a service top-up is due
   *
   */
}
