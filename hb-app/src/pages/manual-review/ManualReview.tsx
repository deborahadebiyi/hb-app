const ManualReview = () => {
  // requires a form
  // this will be sent to be manually removed
  // if review deems the review unfit a deleteOne operation will take the reviewId
  // otherwise an email will be sent deeming the review fit with a template of tips
  return <div></div>;
};

export default ManualReview;
